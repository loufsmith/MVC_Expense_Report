@model IEnumerable<Cts.Mvc.ExpenseReport.Domain.Titles>

@{
    ViewBag.Title = "Titles";
}

<h2>Titles</h2>

<p>
    <a href="@Url.Action("/Create")" role="button" class="btn btn-default">Add Title</a>
</p>
<div class="row-fluid">
    <div class="span6">
        <table class="table table-striped table-condensed table-bordered">
            <tr>
                <th width="2%"></th>
                <th width="98%">Title</th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a href="@Url.Action("Delete", new { id = item.TitlesID })"><i class="icon-trash" title="Delete"></i></a>
                    </td>
                    <td>
                        <a href="@Url.Action("Edit", new { id = item.TitlesID })">@Html.DisplayFor(modelItem => item.Title)</a>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
